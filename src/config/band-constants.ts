/**
 * @fileOverview Shared constants for the band application.
 * This file contains constants that can be safely used across both server and client components.
 */

export const tones = ["Professional", "Energetic", "Edgy", "Brief", "Mythical"] as const;

export const socialPlatforms = ["Facebook", "Instagram"] as const;
